{
  "name": "web-audio-calibration",
  "version": "0.2.0",
  "description": "web audio calibration across devices (gain and delay)",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "*",
    "jade": "*",
    "platform": "*",
    "socket.io": "*"
  }
}
